import { config } from "dotenv";
config();

export const URL = "https://reqres.in/api/";

// export const AUTH_TOKEN = (token: string) => {
//   return { Authorization: ` Bearer ${token}` };
// };

// export const TOKEN_ENCRYPT = {
//   Authorization: `Bearer ${constant.tokenEncrypt}`,
// };
